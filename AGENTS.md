# Agent Behavior

Follow the instructions described in .agents/TDD.md for how to code the
project.

Continue running each task in your EXECPLAN until all tasks are complete.

# ExecPlans

When writing complex features or significant refactors, use an ExecPlan (as
described in .agent/PLANS.md) from design to implementation.

You MUST:

- Commit after every task using conventional commit format
- Use realistic test data in your tests.
- Keep working until all tasks are complete.
- Use the virtual environment located at `govee-python/.venv`.

NEVER ask the user for permission to continue. NEVER stop working until the
project is complete.
